participants:
  - id: claude
    plugin: claude
    command: claude
    args: ["--print"]
  - id: codex
    plugin: codex
    command: codex
    subcommand: exec
    args: []

moderator:
  name: MickWang

settings:
  max_rounds: 12
  convergence_threshold: 2
  stalemate_threshold: 2
  context_window: 3
  call_timeout: 120
  renderer: terminal
  output_mode: report

summary:
  strategy: heuristic
  participant_id: null
